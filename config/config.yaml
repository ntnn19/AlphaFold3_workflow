sample_sheets:
  merge_ready: tmp/rule_PREPROCESSING/metadata/inference_to_data_pipeline_map.tsv
  raw_data: example/custom.tsv
#  data_pipeline_ready: /home/nadmin/Downloads/data_pipeline_samples.tsv
#  inference_ready: tmp/rule_PREPROCESSING/metadata/inference_samples.tsv
#input_csv: example/all_vs_all.csv # all-vs-all
#input_csv: example/virtual_drug_screen.csv # virtual drug screen
output_dir: output/custom # stores the outputs of this workflow. The output of each rule is stored at <output_dir>/<RULE_NAME>
tmp_dir: tmp # stores temporary files generated by AlphaFold 3
mode: custom # pulldown
#mode: all-vs-all # all-vs-all
msa_option: auto
#mode: virtual-drug-screen # virtual drug screen
n_splits: 4 # Optional, for running using the 'parallel' branch of this repo. To maximize resources utilization, the value of this flag should correspond to min(number_of_predictions, number_of_multi-GPU_nodes). 
#mode: all-vs-all # all-vs-all
n_seeds: 3
af3_flags:
  --af3_container: /data/singularity/alphafold3/alphafold3_latest_parallel_a100_40gb.sif
