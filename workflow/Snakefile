"""
AlphaFold 3 Snakemake Workflow
==============================
A modular, reproducible pipeline for high-throughput AF3 structure prediction.
"""

import os
from pathlib import Path

# ── Workflow directory (used in shell commands) ──────────────────────────────
WORKFLOW_DIR = os.path.dirname(os.path.abspath(workflow.snakefile))

# ── Shared helpers, constants, and input functions ───────────────────────────
include: "rules/common.smk"

# ── Rule modules ─────────────────────────────────────────────────────────────
include: "rules/preprocessing.smk"
include: "rules/data_pipeline.smk"
include: "rules/merge_msas.smk"
include: "rules/mutate.smk"
include: "rules/inference.smk"
include: "rules/ipsae.smk"
include: "rules/aggregate.smk"
include: "rules/report.smk"

# ── Local rules ──────────────────────────────────────────────────────────────
localrules: PREPROCESSING, MERGE_MONO_AND_MULTI_JSON, AGGREGATE_RESULTS, DATAVZRD_REPORT, MUTATE

if config.get("run_data_pipeline_locally", False):
    localrules: AF3_DATA_SPEEDY_PIPELINE,PREPROCESSING, MERGE_MONO_AND_MULTI_JSON, AGGREGATE_RESULTS, DATAVZRD_REPORT, MUTATE

# ── Wildcard constraints ─────────────────────────────────────────────────────
wildcard_constraints:
    mono  = r"[A-Za-z0-9_\-\.]+",
    multi = r"[A-Za-z0-9_\-\.]+",
    split = r"\d+",

# ── Target rule ──────────────────────────────────────────────────────────────
_raise_nofile_limit()

prepare_container_binds(
    workflow_directory=WORKFLOW_DIR,
    output_directory=config["output_dir"],
    config=config
)

rule all:
    """Collect all final outputs."""
    input:
        inference_outputs,
        datavzrd_output
    default_target: True
